export type UrlsDTO = {
    raw: string;
    full: string;
    regular: string;
    small: string;
    thumb: string;
};

export type LinkDTO = {
    self: string;
    html: string;
    download: string;
    download_location: string;
};

export type TagDTO = {
    type: string;
    title: string;
};
